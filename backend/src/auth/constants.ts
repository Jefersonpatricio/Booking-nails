export const ACCESS_TOKEN_COOKIE = 'access_token';

// Mesmo período usado como default de JWT_EXPIRES_IN — mantenha os dois em sincronia.
export const ACCESS_TOKEN_COOKIE_MAX_AGE_MS = 7 * 24 * 60 * 60 * 1000;
